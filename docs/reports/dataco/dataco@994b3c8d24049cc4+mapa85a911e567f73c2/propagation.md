# Propagation Report

- standing: `STATED`

> Every link walked below is one the engine asserts. It is still not proof of causation: this engine measures support, and no part of it identifies a causal effect.

- `report_schema_version`: `1.0.0`
- `run_id`: `run:f5e7ab410fa9eeb4`
- seed: `evt:015bd166df4b2162`
- links available in this view: 0

## What this report does not contain

The traversal ran to exhaustion within its declared bounds. That is a statement about this graph and these bounds, not about the world: a consequence the graph never claimed is a consequence this report cannot see, and the graph is smaller than the truth.

## Depth and breadth

prd.md §30's two structural measures, as two numbers. They are never averaged: a deep narrow chain and a shallow wide fan-out are different problems and one figure could not tell them apart.

- propagation depth: **0**
- propagation breadth (widest single level): **0**
- consequences reached: **0**
- affected entities: **0**
- affected process instances: **0**

## Magnitude



**No total.** the traversal reached no consequence, so there is nothing to combine. Whether that is because the graph is empty, because the seed has no onward link, or because no depth was declared is stated on the tree.

This is an absence, not a figure of zero.

## Route confidence

Composed under `nothing -- no route was composed`. A chain is only as strong as its weakest link; the length-sensitive product is carried beside every composed value in the JSON artifact and is never blended with it.

## Where the traversal stopped

Nowhere: the traversal ran to exhaustion within its bounds.

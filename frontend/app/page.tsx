/**
 * Placeholder shell. The six workspaces (prd.md §51) are module 16 work and are not
 * built yet; nothing here calls the API, because no API exists to call.
 */
export default function Home() {
  return (
    <main>
      <h1>CausaLog</h1>
      <p>Scaffold only. No workspace is implemented yet.</p>
    </main>
  );
}

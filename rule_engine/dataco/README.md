# `rule_engine/dataco`

**Single responsibility:** hold the rule pack for the DataCo ontology instance.

Authored with module 9 (Candidate Cause Generator), which is the first module that consumes
rules. Empty until then, by intent — an unratified rule would fire against a graph nobody
has validated.
